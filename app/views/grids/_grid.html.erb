<table class="table grid-table shadow-sm">
  <tbody>
    <% grid.cells.each_slice(Grid::SIZE) do |line| %>
      <tr>
        <% line.each do |cell| %>
          <td class="<%= cell_class(cell)%>"></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>